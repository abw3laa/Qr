# تصميم QR Card

## الرؤية

منصة عربية عصرية تنشئ بطاقة شخصية رقمية قابلة للمشاركة عبر QR. تعمل البطاقة بطريقتين: **وضع offline** عبر تضمين البيانات الأساسية داخل رمز QR، و**وضع online** عبر فتح صفحة عامة قابلة للتحديث والمزامنة بين الموقع وتطبيق أندرويد.

## Screen List

| الشاشة | المحتوى والوظائف الأساسية |
|---|---|
| الترحيب | تعريف سريع بالقيمة، زر إنشاء البطاقة، وزر تسجيل الدخول/المزامنة عند الحاجة. |
| لوحة البطاقات | عرض البطاقات المحفوظة، حالة المزامنة، إنشاء بطاقة جديدة، وفتح بطاقة للتحرير أو المشاركة. |
| محرر البطاقة | الاسم، المسمى الوظيفي، الشركة، نبذة، الهاتف، البريد، الموقع، صورة شخصية اختيارية، وروابط الشبكات الاجتماعية. يدعم ترتيب الروابط وإخفاء أي حقل. |
| المعاينة | معاينة البطاقة كما يراها الزائر، مع تبديل الوضع الفاتح/الداكن، وإنشاء QR، ومشاركة الرابط أو الرمز. |
| شاشة QR | رمز QR كبير مع خيارين: QR كامل يعمل دون إنترنت، وQR رابط يفتح الصفحة العامة عند توفر الشبكة، مع تنزيل/مشاركة الرمز. |
| البطاقة العامة | صورة واسم ومهنة وروابط تواصل وأزرار اتصال ومتابعة وحفظ جهة الاتصال. تظهر البيانات المضمّنة دون اتصال، وتُحدّث من الخادم عند الاتصال. |
| المسح | استخدام كاميرا الهاتف لمسح QR وعرض النتيجة، مع دعم فتح QR من صورة عند الإمكان. |
| الإعدادات | الحساب والمزامنة، اللغة، المظهر، الخصوصية، حذف البيانات المحلية، وتصدير البطاقة. |

## Primary Content and Functionality

تُصمم الواجهة بتركيز على الهاتف العمودي والاستخدام بيد واحدة، مع شريط تنقل سفلي واضح وأزرار رئيسية ثابتة قرب أسفل الشاشة. تستخدم البطاقة مساحة بيضاء أو سطحاً داكناً مرتفعاً، وشارة حالة صغيرة توضّح إن كانت البيانات محفوظة محلياً أو تمت مزامنتها.

الحقول الرئيسية ستُحفظ في نموذج مشترك بين الويب والتطبيق. روابط التواصل عبارة عن قائمة مرتبة تحتوي على نوع الرابط، عنوانه، عنوان العرض، وحالة الظهور. عند الضغط على **إنشاء QR** يُنتج التطبيق تمثيلاً مضغوطاً ومُرمّزاً للبيانات الأساسية حتى يمكن قراءته بلا إنترنت، مع خيار QR قصير للرابط العام عند توفر المزامنة.

## Key User Flows

### إنشاء بطاقة

1. يفتح المستخدم التطبيق أو الموقع ويضغط «إنشاء بطاقة».  
2. يدخل الاسم والعمل والهاتف والروابط الاجتماعية.  
3. يضغط «معاينة» ليرى البطاقة العامة.  
4. يضغط «إنشاء QR» ويختار QR يعمل دون إنترنت أو QR للرابط العام.  
5. تُحفظ البطاقة محلياً فوراً، ثم تُزامن عند توفر الإنترنت إذا كان المستخدم مسجلاً.

### مشاركة البطاقة

1. يفتح المستخدم شاشة QR.  
2. يضغط مشاركة أو تنزيل.  
3. يرسل الصورة أو الرابط عبر تطبيق المشاركة في النظام.  
4. يستطيع المستلم فتح البطاقة العامة أو مسح الرمز بكاميرا الهاتف.

### استقبال البطاقة وحفظ جهة الاتصال

1. يمسح المستلم الرمز بالكاميرا.  
2. إذا كان QR كاملاً، تُفك البيانات محلياً دون طلب شبكة؛ وإذا كان QR رابطاً، تُفتح الصفحة العامة مع حالة تحميل واضحة.  
3. تظهر أزرار «متابعة» و«حفظ جهة الاتصال» و«الاتصال» و«مراسلة».  
4. ينزّل المستلم ملف vCard أو يضيف جهة الاتصال من خلال نظام التشغيل عند دعم المنصة.

### المزامنة

1. تُكتب التغييرات أولاً إلى التخزين المحلي.  
2. عند عودة الاتصال تُرسل التغييرات المؤجلة إلى الخادم.  
3. تُحدّث البطاقة على الموقع والتطبيق بعد نجاح المزامنة.  
4. تُعرض آخر حالة مزامنة وتُعالج التعارضات باستخدام وقت التعديل الأحدث مع الاحتفاظ بنسخة محلية قابلة للاسترجاع.

## Color Choices

الهوية البصرية تعتمد على كحلي عميق `#0B1020` للخلفيات الداكنة، وأبيض دافئ `#F8FAFC` للخلفيات الفاتحة، وتركوازي مضيء `#22D3C5` للإجراءات الأساسية، وأزرق كهربائي `#5B7CFA` للروابط والثقة، مع نص أساسي `#111827` ونص ثانوي `#64748B`. تستخدم حالات النجاح `#22C55E` والتحذير `#F59E0B` والخطأ `#EF4444` باعتدال.

يجب أن يكون التباين مناسباً للقراءة، وأن تُستخدم الزوايا الدائرية المتوسطة والظلال الخفيفة بدلاً من الزخرفة الثقيلة. الحركة مقتصرة على انتقالات قصيرة لحالات الحفظ والإنشاء، مع رد فعل ضغط واضح ولمسات اهتزاز خفيفة على الهاتف.

## قرارات تقنية مبدئية

يستخدم المشروع Expo للنسخة المحمولة ونسخة الويب المشتركة، مع تخزين محلي AsyncStorage، وطبقة API وقاعدة بيانات للمزامنة عبر الأجهزة. لا تُحفظ البيانات الحساسة في QR إلا إذا اختار المستخدم ذلك؛ ويُنصح بأن تكون البطاقة العامة هي الافتراضي وأن يختار المستخدم الحقول المشمولة في QR الكامل.
