# دليل نشر QR Card بالتفصيل

## النتيجة المتوقعة

بعد تنفيذ الخطوات التالية سيكون لديك رابط عام للموقع، وبطاقة أونلاين تفتح من QR ويمكن تحديثها لاحقاً، وملف Android APK مرفوع داخل GitHub Release. البناء يتم من مستودع [abw3laa/Qr](https://github.com/abw3laa/Qr) عبر GitHub Actions، ولا يحتاج مسار البناء الحالي إلى `EXPO_TOKEN` أو حساب Expo/EAS.

| الجزء | مكان التنفيذ | الناتج |
|---|---|---|
| نشر الموقع | لوحة إدارة المشروع | رابط ويب عام دائم |
| ربط QR الأونلاين | GitHub Actions Variables | QR يفتح البطاقة من الرابط العام |
| فحص المشروع | GitHub Actions | TypeScript وlint واختبارات ناجحة |
| بناء Android | GitHub Runner وGradle | `app-debug.apk` |
| الإصدار | GitHub Release | APK قابل للتنزيل والمشاركة |

## أولاً: نشر الموقع من لوحة المشروع

افتح مشروع QR Card في لوحة إدارة Manus، ثم اضغط زر **Publish** الموجود في أعلى لوحة الإدارة. اتبع نافذة النشر حتى يكتمل بناء نسخة الويب. بعد النجاح سيظهر رابط عام للموقع؛ انسخه كما هو، ويفضل أن يبدأ بـ `https://` وألا ينتهي بشرطة مائلة `/`.

لا تستخدم رابط المعاينة الذي يبدأ عادةً برقم المنفذ `8081` كرابط نهائي لرمز QR، لأنه رابط مؤقت للمراجعة وقد يتغير. استخدم رابط النشر العام فقط.

> ملاحظة: لا أستطيع الضغط على زر Publish بدلاً منك. يجب أن تنفذ هذه الخطوة من لوحة المشروع، ثم تستخدم الرابط الذي يظهر لك في الخطوة التالية.

## ثانياً: إضافة رابط الموقع إلى GitHub

افتح مستودع [QR Card على GitHub](https://github.com/abw3laa/Qr)، ثم انتقل إلى **Settings → Secrets and variables → Actions → Variables**. اضغط **New repository variable** وأدخل القيم التالية:

| الحقل | القيمة |
|---|---|
| Name | `EXPO_PUBLIC_WEB_BASE_URL` |
| Value | رابط الموقع المنشور، مثل `https://example.com` |

اضغط **Add variable**. هذا متغير عادي وليس سراً، لأنه يمثل عنوان الموقع العام فقط. لا تضع رابط المعاينة المؤقت هنا.

## ثالثاً: تشغيل GitHub Actions

افتح تبويب **Actions** داخل المستودع، ثم اختر workflow باسم **QR Card Build**. اضغط **Run workflow**، اختر الفرع `main`، ثم اضغط **Run workflow** مرة أخرى.

يقوم workflow بالخطوات التالية بالترتيب:

1. يثبت الاعتمادات باستخدام pnpm.
2. يفحص TypeScript.
3. يشغّل lint.
4. يشغّل اختبارات QR والمزامنة والتخزين المحلي.
5. يصدر نسخة الويب ويرفعها كـ artifact باسم `qr-card-web`.
6. ينشئ مشروع Android من Expo داخل GitHub Runner.
7. يبني `app-debug.apk` بواسطة Gradle.
8. يرفع APK كـ artifact باسم `qr-card-android-apk`.
9. ينشئ GitHub Release تلقائياً ويرفق APK به إذا كان التشغيل من `main`.

انتظر حتى تصبح وظائف **Validate TypeScript, lint and tests** و**Export web build** و**Build installable Android APK with Gradle** باللون الأخضر. إذا فشلت وظيفة، افتحها ثم افتح الخطوة الحمراء واقرأ أول رسالة `Error`؛ لا تحمّل APK من تشغيل فاشل.

## رابعاً: فتح GitHub Release وتنزيل APK

بعد نجاح workflow، انتقل إلى تبويب **Releases** في المستودع. سيظهر إصدار باسم قريب من `QR Card v1.0.X`، حيث يمثل `X` رقم تشغيل GitHub. افتح الإصدار، ثم من قسم **Assets** اضغط على `app-debug.apk` لتنزيله.

يمكن أيضاً تنزيل الملف من صفحة تشغيل workflow عبر قسم **Artifacts**، لكن رابط Release هو الخيار الأفضل للمشاركة والاحتفاظ بالإصدارات السابقة.

رابط صفحة الإجراءات هو:

[QR Card GitHub Actions](https://github.com/abw3laa/Qr/actions)

ورابط الإصدارات هو:

[QR Card GitHub Releases](https://github.com/abw3laa/Qr/releases)

## خامساً: تثبيت APK على هاتف Android

انقل `app-debug.apk` إلى الهاتف، ثم افتحه من مدير الملفات. إذا طلب Android السماح بالتثبيت من هذا المصدر، افتح إعدادات السماح للتطبيق الذي فتح الملف، ثم فعّل **السماح من هذا المصدر**، وارجع إلى الملف واضغط **تثبيت**.

بعد التثبيت افتح QR Card وتحقق من الشاشة الرئيسية. نسخة debug مناسبة للاختبار والتوزيع المحدود. قبل النشر التجاري على Google Play يلزم إعداد توقيع release رسمي ومفتاح توقيع محفوظ في GitHub Secrets؛ لا تستخدم مفتاحاً تجريبياً للتوزيع التجاري.

## سادساً: اختبار الوظائف بعد النشر

نفّذ الاختبارات التالية على الموقع وAPK، واحتفظ بالنتيجة قبل اعتبار الإصدار جاهزاً:

| الاختبار | طريقة التحقق | النتيجة المطلوبة |
|---|---|---|
| QR أوفلاين vCard | اختر vCard ثم امسح الرمز من كاميرا هاتف آخر | تظهر بيانات الاتصال دون إنترنت |
| QR أوفلاين MeCard | اختر MeCard ثم امسح الرمز | تُقرأ البيانات أو يظهر الإدخال اليدوي البديل |
| البطاقة الأونلاين | اختر Online ثم افتح QR | تفتح صفحة `/c/{slug}` من النطاق العام |
| التحديث | غيّر الاسم أو المهنة من التطبيق/الموقع ثم احفظ | يظهر التحديث عند فتح الرابط نفسه |
| العمل دون إنترنت | عطّل Wi‑Fi والبيانات ثم افتح التطبيق | تبقى البطاقة المحلية قابلة للعرض والتعديل |
| المزامنة | عدّل البطاقة دون إنترنت ثم أعد الاتصال | يُرسل التغيير عند عودة الشبكة |
| حفظ جهة الاتصال | اضغط حفظ جهة الاتصال واسمح بالصلاحية | تظهر البطاقة في جهات اتصال Android |
| الكاميرا | افتح مسح QR واسمح بالكاميرا | يعمل المسح، ومع الرفض يظهر الإدخال اليدوي |
| NFC | على جهاز يدعم NFC اكتب رابطاً أو vCard ثم اقرأه | تتم القراءة والكتابة، وQR يبقى البديل |
| الإعدادات | افتح الإعدادات وبدّل الوضع الليلي | تتغير إعدادات الثيم دون تعطل التطبيق |

## سابعاً: عند تغيير رابط الموقع

إذا تغير رابط الموقع بعد إعادة النشر، حدّث قيمة `EXPO_PUBLIC_WEB_BASE_URL` من GitHub، ثم شغّل workflow مرة أخرى. يجب إعادة بناء APK لأن رابط QR الأونلاين يُضمّن أثناء بناء التطبيق. أما QR الديناميكي المطبوع سابقاً فيبقى صالحاً إذا ظل النطاق والـ slug ثابتين.

## ثامناً: مشاكل شائعة

| المشكلة | الحل |
|---|---|
| لا يظهر GitHub Release | تأكد أن التشغيل جاء من `main` وأن وظيفة الويب وAndroid نجحتا؛ وظيفة Release لا تعمل في Pull Request الفاشل |
| فشل تصدير الويب | افتح سجل وظيفة Export web وتحقق من الخطوة الحمراء، ثم أعد التشغيل بعد دفع الإصلاح |
| فشل Gradle | افتح سجل Build debug APK؛ غالباً تكون المشكلة من إعداد Android أو plugin، ولا تستخدم APK غير مكتمل |
| QR الأونلاين لا يفتح | تأكد من `EXPO_PUBLIC_WEB_BASE_URL` وأنه رابط نشر عام وليس رابط معاينة |
| لا تظهر بيانات من السيرفر | سجّل الدخول من الإعدادات، وتحقق من اتصال الإنترنت وصلاحية API |
| NFC غير متوفر | فعّل NFC من إعدادات الجهاز أو استخدم QR؛ لا يعمل NFC على كل الأجهزة أو المتصفحات |
| حفظ جهة الاتصال لا يعمل | امنح صلاحية جهات الاتصال في Android، أو استخدم مشاركة vCard كبديل |

## مراجع تقنية

[1]: https://docs.github.com/en/actions "GitHub Actions documentation"
[2]: https://docs.expo.dev/workflow/prebuild/ "Expo Prebuild documentation"
[3]: https://developer.android.com/build/building-cmdline "Android command-line build documentation"
[4]: https://docs.github.com/en/repositories/releasing-projects-on-github/about-releases "About releases on GitHub"
