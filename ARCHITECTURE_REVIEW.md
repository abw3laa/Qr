# وثيقة مراجعة مشروع QR Card

## 1. نطاق المنتج

QR Card منصة عربية لإنشاء بطاقة تعريف رقمية شخصية تحتوي على الاسم، المهنة، الشركة، رقم الهاتف، البريد الإلكتروني، الصورة، النبذة، الموقع، وروابط التواصل الاجتماعي. سيعمل المنتج من خلال **تطبيق أندرويد** و**موقع ويب متجاوب** يتصلان بقاعدة بيانات وواجهة API مشتركة.

يتكون المنتج من مسارين متكاملين:

| المسار | الهدف | الحاجة إلى الإنترنت |
|---|---|---|
| أوفلاين | مشاركة بيانات الاتصال الأساسية داخل QR بصيغة vCard أو MeCard، وقراءة الرمز وحفظ الجهة | لا يحتاج الإنترنت بعد إنشاء الرمز |
| أونلاين | مشاركة رابط عام قصير لبطاقة قابلة للتحديث مع صورة وروابط وأزرار تفاعل | يحتاج الإنترنت لفتح الصفحة وتحديث البيانات |

سيكون لكل بطاقة خياران منفصلان لإنشاء QR. هذا أفضل من وضع كل البيانات والرابط في رمز واحد، لأن QR الأوفلاين يجب أن يحمل البيانات فعلياً، بينما QR الأونلاين يجب أن يبقى قصيراً وثابتاً حتى يمكن تعديل البطاقة لاحقاً دون إعادة طباعته.

## 2. الهيكل المقترح للمشروع

```text
qr-card/
├── app/                         # شاشات Expo Router المشتركة للتطبيق والويب
│   ├── (tabs)/                  # لوحة البطاقات، إنشاء، الإعدادات
│   ├── card/[slug].tsx          # صفحة البطاقة العامة الأونلاين
│   ├── editor/[id].tsx          # محرر البطاقة
│   ├── qr/[id].tsx              # توليد وعرض QR
│   ├── scanner.tsx              # مسح QR بالكاميرا
│   ├── nfc.tsx                  # قراءة وكتابة NDEF على Android
│   └── oauth/                   # مسار تسجيل الدخول الموجود في القالب
├── components/                  # مكونات البطاقة والأزرار والنماذج
├── lib/
│   ├── offline-store.ts         # AsyncStorage وطابور المزامنة
│   ├── qr-codec.ts              # vCard/MeCard والترميز وفك الترميز
│   ├── nfc.ts                   # طبقة NFC مع fallback للويب والأجهزة غير المدعومة
│   └── sync.ts                  # مزامنة محلية/خادم
├── shared/
│   ├── card.ts                  # أنواع البطاقة والروابط
│   └── validators.ts             # مخططات Zod المشتركة
├── server/
│   ├── routers.ts               # إجراءات API عبر tRPC
│   ├── db.ts                    # استعلامات قاعدة البيانات
│   └── storage.ts               # تخزين الصور عند الحاجة
├── drizzle/
│   ├── schema.ts                # جداول المستخدمين والبطاقات والروابط
│   └── migrations/              # ترحيلات قاعدة البيانات
├── tests/                       # اختبارات الترميز والمزامنة وAPI
├── .github/workflows/build.yml  # فحص وبناء الويب وAPK عبر EAS
├── eas.json                     # إعدادات APK وAndroid App Bundle
└── app.config.ts                # إعدادات Expo والصلاحيات والهوية
```

النسخة الحالية بدأت من قالب Expo SDK 54 الذي يدعم React Native وReact Native Web وExpo Router وTypeScript وNativeWind وTanStack Query وtRPC وDrizzle. مستودع GitHub المرسل كان فارغاً عند الفحص، لذلك سيصبح المشروع الناتج هو نقطة الانطلاق الفعلية للمستودع.

## 3. التقنيات المستخدمة

| الطبقة | التقنية | سبب الاختيار |
|---|---|---|
| تطبيق Android والويب | Expo SDK 54، React Native، React Native Web | مشاركة معظم الواجهات والمنطق بين التطبيق والموقع |
| التنقل | Expo Router | مسارات واضحة للبطاقة العامة والمحرر وQR وNFC |
| اللغة والتصميم | TypeScript، NativeWind، RTL عربي | نماذج وأنواع آمنة وتصميم متجاوب موحد |
| التخزين المحلي | AsyncStorage | حفظ البطاقة والرموز وطابور التغييرات دون اتصال |
| الحالة وطلبات الخادم | TanStack Query + tRPC | تخزين مؤقت، إعادة المحاولة، وتطابق الأنواع بين العميل والخادم |
| الخادم | Node.js/Express ضمن قالب WebDev + tRPC | API مشتركة للموقع والتطبيق |
| قاعدة البيانات | MySQL عبر Drizzle ORM | متوافق مع مخطط القالب الحالي والترحيلات النوعية |
| QR | qrcode + مكوّن QR متوافق مع React Native SVG | توليد QR على Android والويب من نفس منطق الترميز |
| مسح QR | expo-camera | مسح QR من كاميرا الهاتف مع معالجة تكرار النتائج |
| جهات الاتصال | expo-contacts | إضافة بطاقة vCard إلى دفتر الهاتف بعد موافقة المستخدم |
| NFC | react-native-nfc-manager | قراءة وكتابة NDEF على Android؛ يتطلب Development Build أو APK وليس Expo Go |
| المشاركة | expo-sharing وواجهة مشاركة النظام | مشاركة صورة QR أو رابط البطاقة |
| البناء | GitHub Actions + EAS | فحص تلقائي وبناء APK عند الطلب باستخدام GitHub Secret باسم EXPO_TOKEN |

يجب أن تكون كل ميزات NFC وكتابة جهات الاتصال محمية بفحص المنصة والصلاحيات. مكتبة NFC هي وحدة أصلية، ولذلك لن تعمل في Expo Go العادي؛ سيتم اختبارها داخل APK أو Development Build مخصص. يدعم Android سجلات NDEF من نوع URI وText ويمكنه تمرير URI إلى التطبيق أو المتصفح وفق نظام dispatch الرسمي [1].

## 4. تصميم قاعدة البيانات

### جدول `users`

الجدول موجود في القالب ويخزن هوية المستخدم المرتبطة بتسجيل الدخول.

| الحقل | النوع | الوصف |
|---|---|---|
| `id` | int | المفتاح الأساسي |
| `openId` | varchar | معرف OAuth الفريد |
| `name` | text | اسم المستخدم |
| `email` | varchar | البريد |
| `createdAt` | timestamp | وقت الإنشاء |
| `updatedAt` | timestamp | وقت آخر تعديل |

### جدول `cards`

| الحقل | النوع | الوصف |
|---|---|---|
| `id` | int | المفتاح الأساسي |
| `userId` | int | مالك البطاقة |
| `publicSlug` | varchar unique | الجزء القصير من الرابط العام |
| `name` | varchar | الاسم الظاهر |
| `jobTitle` | varchar | المهنة أو المسمى |
| `company` | varchar nullable | الشركة |
| `bio` | text nullable | النبذة |
| `phone` | varchar nullable | الهاتف |
| `email` | varchar nullable | البريد |
| `location` | varchar nullable | الموقع |
| `avatarUrl` | text nullable | رابط الصورة |
| `theme` | json | الألوان والخطوط وخيارات المظهر |
| `isPublished` | boolean | هل البطاقة العامة منشورة |
| `version` | int | رقم إصدار المزامنة |
| `updatedAt` | timestamp | آخر تعديل على الخادم |
| `createdAt` | timestamp | وقت الإنشاء |

### جدول `card_links`

| الحقل | النوع | الوصف |
|---|---|---|
| `id` | int | المفتاح الأساسي |
| `cardId` | int | البطاقة المالكة |
| `platform` | varchar | linkedin أو instagram أو غيرهما |
| `label` | varchar | النص الظاهر للزائر |
| `url` | text | الرابط |
| `sortOrder` | int | ترتيب العرض |
| `isVisible` | boolean | إظهار أو إخفاء الرابط |

### جدول `sync_changes`

| الحقل | النوع | الوصف |
|---|---|---|
| `id` | int | المفتاح الأساسي |
| `userId` | int | صاحب التغيير |
| `cardId` | int | البطاقة المتأثرة |
| `clientMutationId` | varchar unique | يمنع تكرار التغيير عند إعادة المحاولة |
| `payload` | json | التغيير المنظم |
| `baseVersion` | int | إصدار العميل قبل التعديل |
| `createdAt` | timestamp | وقت تسجيل التغيير |

لن تُحفظ بيانات البطاقة الخاصة داخل QR الأونلاين؛ سيحمل الرمز معرفاً عاماً فقط. أما QR الأوفلاين فيُنشأ محلياً من الحقول التي يحددها المستخدم.

## 5. تصميم API المشتركة

| الإجراء | النوع | الوظيفة |
|---|---|---|
| `cards.list` | query محمي | جلب بطاقات المستخدم |
| `cards.getBySlug` | query عام | جلب البطاقة العامة المنشورة |
| `cards.create` | mutation محمي | إنشاء بطاقة جديدة |
| `cards.update` | mutation محمي | تعديل البطاقة والنسخة |
| `cards.publish` | mutation محمي | نشر أو إخفاء البطاقة العامة |
| `cards.delete` | mutation محمي | حذف البطاقة |
| `sync.pull` | query محمي | جلب تغييرات الخادم منذ نسخة محددة |
| `sync.push` | mutation محمي | إرسال التغييرات المؤجلة مع معرف منع التكرار |
| `media.uploadUrl` | mutation محمي | إنشاء رابط رفع للصورة عند تفعيل الصور السحابية |

صفحة البطاقة العامة `cards.getBySlug` ستكون عامة للقراءة فقط، بينما كل عمليات الإنشاء والتعديل والمزامنة ستكون محمية بتسجيل الدخول. لا يحتاج الزائر الذي يمسح QR الأونلاين إلى حساب.

## 6. QR الأوفلاين

### vCard

يُنشأ نص vCard محلياً وفق صيغة اتصال قياسية، ويضم الاسم ورقم الهاتف والبريد والشركة والمسمى والموقع وروابط مختارة. مثال منطقي مبسط:

```text
BEGIN:VCARD
VERSION:3.0
FN:الاسم الكامل
ORG:اسم الشركة
TITLE:المهنة
TEL;TYPE=CELL:+000000000
EMAIL:person@example.com
URL:https://example.com
END:VCARD
```

عند مسح QR بكاميرا Android أو داخل التطبيق، يمكن للنظام التعرف على محتوى الاتصال. داخل التطبيق ستُفك البيانات أيضاً وتظهر شاشة تأكيد قبل حفظها. زر الحفظ يستخدم `expo-contacts` بعد طلب الإذن، مع fallback لإنشاء ملف vCard أو مشاركة النص إذا لم تكن صلاحية جهات الاتصال متاحة. توثيق Expo يوضح أن إضافة جهة اتصال تتطلب صلاحية الكتابة وبناء تطبيق مخصص على Android، ولا يكفي Expo Go في هذا المسار [2].

### MeCard

يُدعم MeCard كصيغة أقصر للأجهزة التي تتعامل معها، مثلاً:

```text
MECARD:N:الاسم الكامل;ORG:اسم الشركة;TEL:+000000000;EMAIL:person@example.com;NOTE:نبذة;;
```

سيتيح محرر QR اختيار الصيغة، مع عرض تقدير لحجم النص لأن QR له سعة محدودة. إذا تجاوزت البيانات الحجم العملي، سيقترح التطبيق تقليل النبذة أو الروابط، بدلاً من إنشاء رمز يصعب مسحه.

### التخزين المحلي

يحفظ التطبيق نسخة البطاقة في AsyncStorage، بالإضافة إلى آخر QR منشأ وتاريخ التعديل وطابور تغييرات غير مرسلة. يمكن تعديل البطاقة دون اتصال ثم إنشاء QR جديد فوراً. لن يُرسل أي تغيير إلى الخادم حتى يصبح الإنترنت متاحاً ويكون المستخدم مسجلاً.

## 7. QR الأونلاين

يُنشأ QR قصير مثل:

```text
https://qr-card.example/c/ahmad-7k2m
```

الجزء الثابت هو `publicSlug`، لذلك يمكن تعديل الاسم أو الصورة أو الروابط من التطبيق أو الموقع دون إعادة طباعة الرمز. عند فتح الرابط، تعرض صفحة عامة متجاوبة الاسم والمهنة والهاتف والبريد والصورة والنبذة والروابط والأزرار التالية:

| الزر | السلوك |
|---|---|
| اتصال | فتح تطبيق الهاتف عبر `tel:` |
| واتساب | فتح رابط WhatsApp المجهز |
| البريد | فتح تطبيق البريد عبر `mailto:` |
| فتح الرابط | فتح الشبكة الاجتماعية أو الموقع |
| مشاركة البطاقة | مشاركة الرابط عبر نظام المشاركة أو Web Share |
| حفظ جهة الاتصال | تنزيل vCard أو فتح نموذج الإضافة على التطبيق |

عند عدم توفر الإنترنت، لا يمكن فتح صفحة البطاقة العامة المحدثة من QR الأونلاين وحده؛ لذلك سيظهر للمستخدم في شاشة الإنشاء خيار QR الأوفلاين عندما يريد ضمان العمل دون اتصال.

## 8. NFC

سيستخدم التطبيق سجلات NDEF من نوع URI أو Text. الوضع الافتراضي المقترح هو كتابة رابط البطاقة العامة القصير إلى الشريحة، لأنه يبقى قابلاً للتحديث دون لمس الشريحة مرة أخرى. ويمكن للمستخدم اختيار «NFC أوفلاين» لكتابة vCard أو MeCard، مع التنبيه إلى محدودية سعة الشريحة وأن بعض الهواتف تفسر URI تلقائياً بينما تحتاج البيانات النصية إلى التطبيق.

مسار الكتابة هو: فحص دعم NFC، تشغيل NFC، تقريب الشريحة، كتابة سجل NDEF، التحقق من نجاح الكتابة، ثم إيقاف جلسة NFC. مسار القراءة هو: اكتشاف الشريحة، تحليل URI أو Text، فتح البطاقة أو عرض الاتصال، ثم إتاحة الحفظ. في الويب أو على جهاز لا يدعم NFC ستُعطّل الأزرار مع توجيه المستخدم إلى QR.

## 9. المزامنة online/offline

سيعمل النظام وفق استراتيجية **Local-first**:

1. يكتب المستخدم التغيير إلى التخزين المحلي مباشرة.
2. تظهر البطاقة محدثة فوراً مع شارة «بانتظار المزامنة».
3. عند عودة الإنترنت، يرسل التطبيق التغييرات إلى `sync.push`.
4. يعيد الخادم النسخة المعتمدة والتغييرات الجديدة.
5. يحدّث التطبيق التخزين المحلي ويظهر «تمت المزامنة».

يتم منع التكرار بواسطة `clientMutationId`. في التعارض، يستخدم النظام مقارنة `version`، ويعرض رسالة للمستخدم إذا عُدّلت البطاقة من جهازين، مع اعتماد آخر نسخة معروفة بعد حفظ نسخة محلية احتياطية. لا تعتمد قراءة QR الأوفلاين أو عرض البيانات المحلية على API أو الخادم.

## 10. صفحات الموقع والتطبيق

| الصفحة | التطبيق | الموقع |
|---|---:|---:|
| الترحيب والتعريف | نعم | نعم |
| تسجيل الدخول | نعم | نعم |
| لوحة البطاقات | نعم | نعم |
| محرر البطاقة | نعم | نعم |
| المعاينة | نعم | نعم |
| QR أوفلاين vCard/MeCard | نعم | نعم، مع تنزيل أو مشاركة |
| QR أونلاين | نعم | نعم |
| مسح QR بالكاميرا | نعم | دعم كاميرا المتصفح عند توفرها، مع إدخال/فتح رابط كبديل |
| قراءة وكتابة NFC | Android APK | غير متاح في المتصفحات كمسار أساسي |
| البطاقة العامة | نعم | نعم، كرابط قابل للفهرسة والمشاركة |
| الإعدادات والمزامنة | نعم | نعم |

سيكون اتجاه الواجهة RTL، مع خط عربي واضح، ونسخة سطح مكتب تستخدم تخطيطاً أوسع من دون تغيير منطق الاستخدام. على الهاتف ستستخدم النماذج أقساماً قصيرة وأزراراً كبيرة قابلة للوصول بيد واحدة.

## 11. الأمان والخصوصية

يُطلب من المستخدم تحديد الحقول التي يسمح بوضعها في QR الأوفلاين، لأن أي بيانات داخل QR يمكن نسخها. لا تُوضع بيانات الدخول أو رموز الجلسات أو معلومات غير مخصصة للنشر داخل QR. تُستخدم صلاحيات الكاميرا وجهات الاتصال وNFC فقط عند بدء الوظيفة، مع شرح واضح باللغة العربية.

## 12. GitHub Actions وAPK

يوجد workflow مقترح في `.github/workflows/build.yml` يقوم بفحص TypeScript وlint والاختبارات، ثم يصدّر نسخة الويب كـ artifact. يمكن تشغيل بناء APK يدوياً من GitHub Actions عبر خيار `build_android`. يحتاج البناء إلى إضافة `EXPO_TOKEN` في GitHub Secrets، وربط مشروع Expo/EAS بحساب البناء. ملف `eas.json` يحدد نسخة preview من نوع APK للتجربة ونسخة production من نوع Android App Bundle للنشر.

## 13. معايير القبول قبل التنفيذ

| المجال | معيار القبول |
|---|---|
| الأوفلاين | إنشاء vCard وMeCard وحفظ البطاقة محلياً وتعديلها دون اتصال |
| المسح | قراءة QR وإظهار البيانات وحفظ جهة اتصال دون اتصال عندما تكون البيانات مضمنة |
| الأونلاين | فتح رابط قصير عام وتحديث البطاقة دون إعادة طباعة QR |
| المزامنة | انتقال التغيير المحلي إلى الخادم عند عودة الإنترنت وتحديث الموقع والتطبيق |
| NFC | قراءة وكتابة NDEF داخل APK على جهاز Android يدعم NFC، مع fallback إلى QR |
| الواجهة | RTL عربي، متجاوبة، حديثة، ودعم الوضعين الفاتح والداكن |
| الجودة | نجاح typecheck وlint والاختبارات، وعدم وجود أزرار غير فعالة |
| البناء | تشغيل workflow يدوياً وإنتاج APK عبر EAS عند توفير `EXPO_TOKEN` |

## القرار المطلوب منك

أحتاج موافقتك على هذه المعمارية قبل الانتقال إلى كتابة ميزات المنتج. الاختيار الأهم هو أن تكون البطاقات الأونلاين قابلة للتحديث عبر رابط ثابت، بينما تكون بطاقات الأوفلاين مستقلة داخل QR وتحتاج إلى إنشاء رمز جديد عند تعديل البيانات. هذا الفصل هو الذي يضمن عمل الوضعين بوضوح وبدون التباس.

## References

[1]: https://developer.android.com/develop/connectivity/nfc/nfc "Android Developers: NFC basics"
[2]: https://docs.expo.dev/versions/latest/sdk/contacts/ "Expo Contacts documentation"
[3]: https://www.npmjs.com/package/react-native-nfc-manager "react-native-nfc-manager package documentation"
[4]: https://github.com/abw3laa/Qr "abw3laa/Qr GitHub repository"
